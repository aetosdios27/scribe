# Medium export fixtures

Archive tests generate synthetic ZIP files in the operating system's temporary
directory. Committed HTML fixtures in this directory must contain no account
data, credentials, analytics identifiers, or private draft content.

An official Medium export is required before claiming real-export schema
compatibility. Keep that export under `.scribe-local/` until it has been reduced
and sanitized.
