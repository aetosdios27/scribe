---
"@scribe-sdk/react": minor
"@scribe-sdk/mdx": minor
"@scribe-sdk/styles": minor
---

Add Mermaid diagram rendering for ` ```mermaid ` code fences, a zoomable lightbox for `ScribeImage` with a `#nozoom` fragment and `zoom={false}` escape hatch, a `Video` component, a word-wrap toggle button on code blocks (with a `wrap` code-fence flag), and `success`/`error` Callout variants.
