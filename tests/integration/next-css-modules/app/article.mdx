# CSS Module host

The host owns this paragraph's serif typography, measure, and rhythm without mapping any Scribe token.

## Explicit element bridge

> Non-inherited blockquote presentation is deliberately applied through the host's CSS Module.

| State | Meaning |
| --- | --- |
| `ready` | Foundation mechanics coexist with host presentation. |

```ts filename="src/state.ts" lineNumbers
export const state = "ready"
```
